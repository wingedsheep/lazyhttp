# OAuth2 helper tour — first-class token fetch + cache + auto-attach.
#
# Instead of hand-rolling a "login" request and `@capture`-ing the token into a
# variable, lazyhttp can honor the IntelliJ HTTP Client's `Security.Auth` blocks
# in http-client.env.json. Reference a configuration with {{$auth.token("id")}}
# and lazyhttp fetches a token from its Token URL, caches it until it expires,
# and reuses it across every request in the plan — one token fetch, not twenty.
#
# This file is also valid in the IntelliJ HTTP Client.
#
# Try it locally with no real provider: start the bundled stub server in one
# terminal and run this plan against it in another —
#     just demo-server      # token endpoint + echo resource on :9000
#     just demo             # lazyhttp --env local example.oauth.http
# The token is fetched once and reused for both requests; each response echoes
# the Authorization header it received.
#
# To run it against a real API, point the "demo" config in http-client.env.json
# at your own OAuth2 provider and export the secret:
#     OAUTH_CLIENT_SECRET=… lazyhttp --env dev example.oauth.http
#
# Supported grant types: Client Credentials, Password, and Authorization Code.
# This file demonstrates the back-channel Client Credentials grant; for the
# interactive, browser-based Authorization Code grant see example.browser.http
# (a complete, no-setup browser sign-in against the bundled demo server). The
# config lives in the env file so
# secrets stay out of the plan; the request preview (`i`) shows the
# {{$auth.token(...)}} placeholder, never the resolved token.

### Call a protected endpoint
# @group OAuth2
# @name Protected request
GET {{api}}/products/1
Authorization: Bearer {{$auth.token("demo")}}

### Reuse the cached token — no second token fetch
# @group OAuth2
# @name Reuses cached token
GET {{api}}/products/2
Authorization: Bearer {{$auth.token("demo")}}
